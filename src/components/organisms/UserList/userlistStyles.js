import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
    root: {
      width: '100%',
      maxWidth: '100vh',
      backgroundColor: 'traslucent',
      position: 'relative',
      overflow: 'auto',
      maxHeight: '91vh',
    },
  }));
  